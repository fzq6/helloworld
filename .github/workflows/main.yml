name: work-name
run-name: run-name
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "#!/bin/bash" >> jb.sh |
             echo " touch Dockerfile" >> jb.sh |
             echo "echo FROM  centos >> Dockerfile" >> jb.sh |
             echo "docker build -f Dockerfile -t helloworld:1.0  ." >> jb.sh |
             echo "docker run --name=helloworld helloworld:1.0" >> jb.sh |
             sh ./jd.sh
      - run: docker logs helloworld
      - run: docker ps -a
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
